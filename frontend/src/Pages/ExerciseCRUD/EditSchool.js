import React, { Component } from 'react'
import { withStyles } from '@material-ui/core'
import { EditSchoolStyles } from '../../Styles/ExerciseCRUD/EditSchoolStyles'

class EditSchool extends Component {
    render() {
        return (
            <React.Fragment>

            </React.Fragment>
        )
    }
}

export default withStyles(EditSchoolStyles, { withTheme: true })(EditSchool)