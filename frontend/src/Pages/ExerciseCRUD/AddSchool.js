import React, { Component } from 'react'
import { withStyles } from '@material-ui/core'

class AddSchool extends Component {
    render() {
        return (
            <React.Fragment>

            </React.Fragment>
        )
    }
}

export default withStyles(AddSchoolStyles, { withTheme: true })(AddSchool)