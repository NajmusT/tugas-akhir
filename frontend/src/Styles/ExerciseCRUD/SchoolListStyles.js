import { Color } from "../../Constants/Colors";
import { FontFamily } from "../../Constants/FontFamily";

export const SchoolListStyles = theme => ({
    root: {
        flexGrow: 1,
    },
    menuButton: {
        marginRight: theme.spacing(2),
    },
    title: {
        flexGrow: 1,
    },
});